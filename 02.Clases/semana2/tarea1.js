//
//
const {ask} = require ('../helpers/input');

async function main() {
    let limite = 100;
    console.log('ciclo while');
    while (contador<=limite);
    if (i % 2 === 0);{
        console.log(`el numero ${i} es par`);

        console.log (`ciclo for 2 en 2`);
        
    }

}